<div class="p-4">
  <div class="w-full h-full border border-red-700 flex justify-between items-center">
    <p>This is Dashboard</p>
    <p>you are logged in as <%= @user.email %></p>
    <% if user_signed_in? %>
    <p><%= link_to "Sign-out", destroy_user_session_path, method: :delete %></p>
    <% end %>
  </div>
</div>
